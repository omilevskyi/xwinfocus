---
name: Cppcheck (lint)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  cppcheck:
    name: Run cppcheck
    if: ${{ github.ref != 'refs/heads/main' }}
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout code
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # 5.0.1
        with:
          fetch-depth: 1
          show-progress: "false"

      - name: Update
        run: sudo apt-get update --yes --quiet=2

      - name: Install
        run: |
          sudo apt-get install --yes --quiet=2 \
            --no-install-recommends --option DPkg::NoTriggers=true cppcheck

      - name: Run cppcheck
        run: |
          cppcheck \
            --enable=all --inconclusive --check-level=exhaustive --force --error-exitcode=1 \
            --language=c --std=c99 --platform=unix64 \
            --suppress=missingIncludeSystem \
            --suppress=checkersReport \
            $(find . -name '*.c' -not -name 'test_*' -type f)
